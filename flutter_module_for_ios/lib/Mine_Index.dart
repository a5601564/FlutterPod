import 'package:flutter/material.dart';
class MinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // return Container();
    return new Scaffold(
      appBar: new AppBar(
        title: new Text('Mine_Index'),
      ),
      body: Text('data'),
    );
  }
}
